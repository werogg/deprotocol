[build-system]
requires = ["setuptools>=61.0", "wheel", "pyinstaller"]
build-backend = "setuptools.build_meta"

[tool.pyinstaller]
console_scripts = ["deprotocol = deprotocol.app.__main__:main"]

[tool.distutils.bdist_wheel]
universal = true
